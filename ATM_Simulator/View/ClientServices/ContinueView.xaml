<UserControl x:Class="ATM_Simulator.View.ClientServices.ContinueView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="500" x:ClassModifier="internal">
    <Grid Name="MainGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="120"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Label Name="Label" Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" FontWeight="Bold" FontSize="25" VerticalAlignment="Bottom" HorizontalAlignment="Center" Content="Do you want to continue you session?"/>

        <Button Grid.Row="1" Grid.Column="0" Content="NO" HorizontalAlignment="Center" VerticalAlignment="Center" Width="150" Height="40" Command="{Binding NoCommand}"/>
        <Button Grid.Row="1" Grid.Column="1" Content="YES" HorizontalAlignment="Center" VerticalAlignment="Center" Width="150" Height="40" Command="{Binding YesCommand}"/>

    </Grid>
</UserControl>
